---
phase: ai-assistant-full-capabilities
plan: docs/superpowers/plans/2026-04-11-ai-assistant-full-capabilities.md
status: code_complete_pending_deploy
last_updated: 2026-04-12T03:17:47Z
---

<current_state>
All 5 implementation tasks from the plan are complete and pushed to main.
The main-service stack (todo-houessou-com) is in UPDATE_ROLLBACK_COMPLETE in us-east-1 —
needs a manual workflow_dispatch re-run to deploy the pipeline fix.
The ai-assistant stack needs its pipeline triggered to deploy the new action group functions.
</current_state>

<completed_work>

- Task 1: AI Action Group (handler.py) — DONE
  - Added deleteTodo, listTodoFiles, addTodoFile, deleteTodoFile functions
  - Fixed: removed duplicate boto3 client, fixed URL decoding (urllib.parse.unquote), handle missing file in deleteTodoFile
  - Commits: 519baa2, 5e8081b

- Task 2: SAM Template (infra/sam/ai-assistant/template.yaml) — DONE
  - Added FILES_TABLE, FILES_BUCKET, FILES_BUCKET_CDN env vars
  - Added IAM perms for files DynamoDB table + S3 bucket
  - Added 4 new FunctionSchema entries (deleteTodo, listTodoFiles, addTodoFile, deleteTodoFile)
  - Updated agent Instruction to describe attachment capabilities
  - Commit: 71bf87a

- Task 3: Frontend API Client (apps/web/src/api.ts) — DONE
  - Added getTodoFiles, registerTodoFile, deleteTodoFile
  - types.ts already had TodoFile/FilesResponse — no changes needed
  - Commit: df08fdc

- Task 4: Frontend Attachment UI — DONE
  - apps/web/src/s3upload.ts — created (Cognito Identity Pool → S3 upload)
  - apps/web/src/ui.ts — renderFiles updated with delete buttons; added showFileUploading/hideFileUploading
  - apps/web/src/pages/home.ts — file load on modal open, upload handler on uploadButton, delete delegation on filesList
  - npm install @aws-sdk/client-s3 @aws-sdk/credential-providers
  - Commit: 2417c20

- Task 5: Chat Drag-and-Drop — DONE
  - apps/web/src/chatbot.ts — initChatDropZone added (with double-registration guard)
  - apps/web/src/config.ts — cdnDomain added (VITE_CDN_DOMAIN)
  - apps/web/src/pages/home.ts — openDrawer calls initChatDropZone
  - apps/web/css/style.css — #chatDrawer.drag-over CSS added
  - Commit: 227a041

- TypeScript fixes: 6c351b6 (wrong import renderFiles from api→ui, removed unused pending vars)

- CI/CD fix (bonus): 41a670d — samconfig region ca-central-1→us-east-1, removed stale s3_bucket
  - Root cause of UPDATE_ROLLBACK: old pipeline used --s3-bucket (ca-central-1) with --region us-east-1

- Build verified: npm run build passes, no TS errors, dist/ produced

- Docs: implementation-log.md and blog-input-summary.md updated with Phase 2 entries

</completed_work>

<remaining_work>

- [ ] VITE_CDN_DOMAIN env var: must be added to the frontend pipeline SSM read step
      SSM param needed: /todo-houessou-com/attachments-service/cdn-domain (or equivalent)
      The attachments-service stack exports TodoFilesBucketCFDomainName — check SSM for the param name
      File to update: .github/workflows/frontend-pipeline.yaml (add VITE_CDN_DOMAIN to SSM reads and build env)

- [ ] Re-trigger main-service pipeline (workflow_dispatch) — stack is UPDATE_ROLLBACK_COMPLETE in us-east-1
      Pipeline is now correct (infra/sam/config.json path fixed, samconfig region=us-east-1)

- [ ] Trigger ai-assistant pipeline to deploy new action group + SAM template changes
      Paths that trigger it: services/ai-assistant/** or infra/sam/ai-assistant/**
      After deploy, Bedrock Agent alias will be re-prepared (AutoPrepare: true in template)

- [ ] Smoke test (manual, per plan Task 6):
      1. Chat: "list my todos", "create a todo", "list files for todo X", "delete todo X"
      2. Modal: open todo → No attachments shows; click + Attachment; upload file; verify in list; delete
      3. Drag-and-drop: drop file on chat drawer; AI asks which todo; reply; verify file on todo

- [ ] Verify attachments-service exports TodoFilesBucketCFDomainName (the CDN domain import in template.yaml)
      Check: aws cloudformation list-exports --region us-east-1 | grep CFDomain

</remaining_work>

<decisions_made>

- addTodoFile takes CDN URL (not S3 key) — agent never handles presigned URLs, cleaner interface
- S3 key extraction in Lambda uses urllib.parse.unquote — robust against any percent-encoded filename
- initChatDropZone uses drawer.dataset.dropzoneInit guard to prevent listener stacking on repeated opens
- --resolve-s3 was NOT added (user intentionally removed it from all pipelines — pipelines work without it)
- samconfig.toml s3_bucket removed from main-service and attachments-service (stale ca-central-1 bucket)

</decisions_made>

<blockers>

- VITE_CDN_DOMAIN not yet wired in frontend pipeline — drag-and-drop will produce wrong file URLs until fixed
- The CloudFormation export name for the CDN domain needs to be verified:
  template.yaml uses !ImportValue "todo-houessou-com-attachments-service-TodoFilesBucketCFDomainName"
  but the actual export name may differ — verify before deploy

</blockers>

<context>
Plan: docs/superpowers/plans/2026-04-11-ai-assistant-full-capabilities.md
Steering: implementation-content-logging-steering-standard-v1.0.md (applied throughout)
Log files: docs/content/implementation-log.md, docs/content/blog-input-summary.md

The code is all done and pushed. The remaining work is purely operational:
deploy triggers + one env var addition (VITE_CDN_DOMAIN) to the frontend pipeline.

The attachments-service and ai-assistant pipelines should both be re-triggered.
The main-service pipeline needs workflow_dispatch to recover from UPDATE_ROLLBACK_COMPLETE.
</context>

<next_action>
Start with: verify the CloudFormation export name for the CDN domain:
  aws cloudformation list-exports --region us-east-1 --no-cli-pager | grep -i "CFDomain\|cdn\|cloudfront"
Then add VITE_CDN_DOMAIN to .github/workflows/frontend-pipeline.yaml SSM read step.
Then trigger the three backend pipelines via workflow_dispatch.
</next_action>
